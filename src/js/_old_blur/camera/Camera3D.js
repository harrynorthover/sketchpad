BLUR.Camera3D = function( width, height ) {
	this.width		= width;
	this.height		= height;

	this.position 	= BLUR.Vertex3( 0, 0, 0 );
	this.fov		= 1000;

	this.lookAt = function( point ) {
		/*
		 * TODO: Write lookAt function.
		 */
	};
};